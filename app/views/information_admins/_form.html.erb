<%= form_with(model: information_admin, local: true) do |form| %>
  <% if information_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(information_admin.errors.count, "error") %> prohibited this information_admin from being saved:</h2>

      <ul>
      <% information_admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="form-group row">
      <div class="col-sm-6 mb-3 mb-sm-0">
        <%= form.label :email %>
        <%= form.text_field :email %>

      </div>
      <div class="col-sm-6">
        <%= form.label :phone %>
        <%= form.text_field :phone %>
      </div>
    </div>
    <div class="form-group row">
      <div class="col-sm-6 mb-3 mb-sm-0">
        <%= form.label :password %>
        <%= form.text_field :password %>
      </div>
      <div class="col-sm-6">
        <%= form.label :password_confirmation %>
        <%= form.text_field :password_confirmation %>
      </div>
    </div>

    <div class="form-actions">
      <%= form.submit class: "btn btn-primary" %>
    </div>
<% end %>
